# Example .env for VideoDownloader

# API key for AI services (GPT-5 mini or preferred model)
OPENAI_API_KEY=your_api_key_here
GPT_MODEL=gpt-5-mini

# Backend server
PORT=3000

# SQLite database file (relative to project root)
DATABASE_FILE=./data/database.sqlite

# Path to yt-dlp binary (if not in PATH)
YT_DLP_PATH=yt-dlp

# Optional: path to ffmpeg/ffprobe (binary or directory). If set, backend passes --ffmpeg-location to yt-dlp
FFMPEG_LOCATION=

# Other toggles
NODE_ENV=development

# DO NOT commit a real .env with secrets to source control.
